{
  "name": "ticketbookingapp",
  "version": "1.0.0",
  "description": "--> Creating the Clone of the BookMyShow\r --> Seat Select Facility\r --> Payment Integration\r --> Testing and Deployment",
  "main": "index.js",
  "scripts": {
    "build": "cd server && npm install  && cd ../client && npm install && npm run build",
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.13.2",
    "nodemon": "^3.1.9",
    "stripe": "^18.0.0"
  }
}
